import React from "react";

import { Section } from "components/Common/SectionStyles";

const FeaturedProjects = () => (
  <div>
    <Section fullHeight nopadding>
      Feature Project
    </Section>
  </div>
);

export default FeaturedProjects;
